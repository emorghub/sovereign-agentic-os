# Trino `query` MCP tool — the governed query engine. Reads the lakehouse marts
# THROUGH central Trino (Iceberg on Polaris); Trino enforces row/column governance
# (the Trino->OPA plugin). No DuckDB/pyiceberg here — that is the sandbox lane.
trino>=0.330,<1
mcp>=1.2,<2
