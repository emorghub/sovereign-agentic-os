# ML agent — LangGraph control flow + an OpenAI-compatible client (LiteLLM gateway).
# MLflow registry is queried over plain HTTP (no SDK import); KServe likewise.
# Pinned, small.
fastapi==0.115.6
uvicorn==0.34.0
langgraph==0.2.61
openai==1.59.6
httpx==0.28.1
