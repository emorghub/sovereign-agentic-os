# data-runner — the ingest service. PyArrow reads the uploaded file (CSV as RAW
# strings — no type coercion; Parquet/JSON keep native types) and PyIceberg writes
# it as a Bronze Iceberg table THROUGH the Polaris REST catalog (same warehouse
# Trino reads). boto3 pulls the object from MinIO. No DuckDB — the sovereign stack
# has one query engine (Trino); ingest reads with PyArrow (already a PyIceberg dep).
pyiceberg[pyarrow]>=0.8,<0.9
boto3>=1.34,<2
